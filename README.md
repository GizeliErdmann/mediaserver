# Installation

```
sudo apt-get -y -qq install git jq moreutils curl > /dev/null && \
git clone -b master https://github.com/andreluisos/mediaserver.git && cd ./mediaserver && \
./mediaserver.sh --install
```
